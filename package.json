{
  "name": "chrono-terminal",
  "version": "1.0.0",
  "description": "Application web temps réel avec interface terminal pour cycles collaboratifs",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "build-all": "npm run install-all && npm run build",
    "start": "cd server && npm start",
    "start-prod": "cd server && NODE_ENV=production npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "deploy": "npm run build-all && npm run start-prod"
  },
  "keywords": ["terminal", "realtime", "collaborative", "react", "socket.io"],
  "author": "Admin",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}