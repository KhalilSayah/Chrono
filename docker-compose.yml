version: '3.8'

services:
  bipseed-39:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - CLIENT_URL=*
      - ADMIN_SECRET_CODE=REBOOT2024
    restart: unless-stopped
    volumes:
      - ./server/.env:/app/server/.env:ro
    networks:
      - bipseed-network

networks:
  bipseed-network:
    driver: bridge